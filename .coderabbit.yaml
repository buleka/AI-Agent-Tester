# yaml-language-server: $schema=https://coderabbit.ai/integrations/schema.v2.json
language: "ru"
early_access: true
reviews:
  model: "claude-3-5-sonnet"
  profile: "assertive"
  request_changes_workflow: true
  path_filters:
    - "!**/*.xml"
    - "!**/*.yaml"
    - "!**/*.d.ts"
    - "!**/*.config.ts"
    - "!components/Ui/**"
    - "!**/node_modules/**"
    - "!**/.nuxt/**"
    - "!**/dist/**"
  path_instructions:
    # Vue компоненты
    - path: "**/*.vue"
      instructions: |
        Проверь Vue 3 компонент для Nuxt 3 проекта как опытный senior разработчик.
        Проект использует JavaScript (не TypeScript).
        🏗️ АРХИТЕКТУРА:
        - Бизнес-логика должна быть в composables, не в компонентах
        - API вызовы только через composables или services, НЕ напрямую fetch/axios
        - Если видишь повторяющуюся логику - предложи вынести в composable
        - Проверь, не дублируется ли эта логика в других компонентах проекта
        ⚡ ПРОИЗВОДИТЕЛЬНОСТЬ:
        - Ищи O(n²) алгоритмы в computed/methods - предложи оптимизацию
        - Проверь, используется ли v-for без :key или с index в качестве key
        - Найди вычисления в template - должны быть в computed
        - Проверь отсутствие watch cleanup и AbortController для fetch
        - Обрати внимание на создание новых функций/объектов в render цикле
        🔒 БЕЗОПАСНОСТЬ:
        - v-html без DOMPurify - критическая XSS уязвимость
        - API ключи, токены, секреты в коде - КРИТИЧНО
        - Логирование чувствительных данных (пароли, токены)
        - Проверь SQL injection риски в динамических запросах
        - User input должен валидироваться и санитизироваться
        🎨 КОМПОЗИЦИЯ API:
        - Предпочитай <script setup> вместо Options API
        - defineProps должен иметь полную валидацию:
          * type: String/Number/Boolean/Array/Object/Function
          * required: true/false
          * default: значение или фабрика
          * validator: функция проверки
        - defineEmits должен быть с описанием событий
        - Computed не должны выполнять side effects (fetch, emit, мутации)
        - Watch должен иметь cleanup в onUnmounted если создает подписки
        - Реактивные переменные должны быть ref/reactive (не let/const для данных)
        🎯 СПЕЦИФИКА NUXT 3:
        - Используй auto-import для компонентов, composables, utils
        - Проверь правильность использования useFetch/useAsyncData вместо fetch
        - navigateTo вместо router.push для навигации
        - useState для shared state между компонентами
        - useRuntimeConfig для env переменных, НЕ process.env напрямую
        📐 TAILWIND CSS:
        - Нет inline стилей (style="..."), только классы
        - Используй design tokens вместо магических чисел (text-lg вместо text-[18px])
        - Проверь responsive дизайн (sm:, md:, lg: префиксы)
        - Избегай дублирования классов - предложи @apply или компонент
        - Проверь нет ли конфликтующих классов (m-4 и m-2 одновременно)
        ♿ ACCESSIBILITY:
        - <img> должен иметь alt (описательный, не пустой)
        - Кнопки и ссылки должны иметь aria-label если нет видимого текста
        - Формы должны иметь <label> связанные с input через for/id
        - Интерактивные элементы должны быть доступны с клавиатуры
        - Проверь color contrast для текста
        - Используй семантичные HTML теги (button, не div с @click)
        📝 JAVASCRIPT БЕЗ TYPESCRIPT:
        - Добавляй JSDoc комментарии для функций с параметрами:
          /**
           * @param {string} userId - ID пользователя
           * @param {Object} options - Опции запроса
           * @returns {Promise<Object>} Данные пользователя
           */
        - Валидируй типы входных параметров в runtime (особенно в props)
        - Проверяй наличие полей перед использованием (optional chaining ?.)
        - Используй default параметры для функций
        - Документируй сложные структуры данных в комментариях
        📊 СРАВНИ С КОДОВОЙ БАЗОЙ:
        - Посмотри как эта же задача решена в других компонентах
        - Если есть похожий composable - предложи его использовать
        - Если паттерн новый - объясни почему он лучше существующих
        - Проверь консистентность стиля кода с остальным проектом
        💡 ДАВАЙ КОНКРЕТНЫЕ ПРИМЕРЫ:
        - Не просто "плохо", а "это O(n²), используй Map для O(n)"
        - Покажи код "было → стало"
        - Объясни ПОЧЕМУ это проблема и какие последствия
        - Ссылайся на конкретные строки кода
        🐛 ЧАСТЫЕ ОШИБКИ В JAVASCRIPT:
        - == вместо === (приведение типов)
        - Мутация props напрямую
        - Забытые return в arrow functions
        - Неправильное использование this в callbacks
        - Async/await без try/catch
        - Promise без обработки ошибок (.catch)
        - Сравнение объектов через == или === (сравнивается ссылка)
    - path: "**/composables/**/*.js"
      instructions: |
        Проверь composable как архитектурный компонент (JavaScript).
        
        🏗️ АРХИТЕКТУРА COMPOSABLE:
        - Имя файла ДОЛЖНО начинаться с "use" (useAuth.js, useProducts.js)
        - Функция должна экспортироваться как export function useXxx()
        - Должен возвращать объект с методами/ref, не выполнять side effects при импорте
        - Не должен импортировать Vue компоненты (.vue файлы)
        - Должен быть переиспользуемым в разных компонентах
        
        📝 JAVASCRIPT BEST PRACTICES:
        - Добавь JSDoc для экспортируемой функции:
          /**
           * Composable для работы с продуктами
           * @returns {Object} API для работы с продуктами
           * @returns {Ref<Array>} returns.products - Список продуктов
           * @returns {Ref<boolean>} returns.isLoading - Состояние загрузки
           * @returns {Function} returns.fetchProducts - Загрузить продукты
           */
        - Валидируй параметры функций в начале
        - Используй destructuring для параметров-объектов
        - Документируй возвращаемую структуру данных
        
        ⚡ ПРОИЗВОДИТЕЛЬНОСТЬ:
        - Проверь нет ли O(n²) в логике обработки массивов
        - Используется ли debounce для частых операций (поиск, скролл)
        - Есть ли мемоизация дорогих вычислений (computed)
        - Cleanup для таймеров, подписок, запросов (AbortController)
        - Не создаются ли новые функции в циклах
        
        🔒 БЕЗОПАСНОСТЬ:
        - API ключи должны быть в useRuntimeConfig(), не в коде
        - Валидация входных данных перед отправкой в API
        - Санитизация HTML перед использованием (DOMPurify)
        - Try/catch для всех async операций
        
        ♻️ ПЕРЕИСПОЛЬЗУЕМОСТЬ:
        - Параметры должны быть гибкими (не хардкод)
        - Опции через объект с default значениями:
          function useData(url, options = {}) {
            const { immediate = true, ...rest } = options
          }
        - Должен работать в разных контекстах
        
        📊 ПРОВЕРЬ КОДОВУЮ БАЗУ:
        - Где используется этот composable (поиск по импортам)
        - Не дублирует ли он логику других composables
        - Какие компоненты зависят от него (breaking changes)
        - Есть ли тесты для этого composable
        
        🐛 ЧАСТЫЕ ОШИБКИ:
        - Забыли вернуть объект из composable
        - Не используют ref/reactive для реактивности
        - Side effects при импорте (должны быть в onMounted)
        - Не очищают ресурсы в onUnmounted
        - Мутируют props или параметры напрямую
        
        💡 ПРИМЕРЫ:
        - Покажи как правильно организовать state
        - Предложи улучшения API интерфейса
        - Укажи на несоответствие с паттернами проекта
    - path: "**/stores/**/*.js"
      instructions: |
        Проверь Pinia store на корректность архитектуры (JavaScript).
        
        🏗️ АРХИТЕКТУРА STORE:
        - Имя должно быть useXxxStore (useAuthStore.js, useCartStore.js)
        - Используй defineStore с composition API стилем (setup function)
        - State должен быть иммутабельным вне actions
        - Сложная логика в actions, не в getters
        - Getters только для вычисляемых значений (computed)
        
        📝 СТРУКТУРА STORE:
        export const useMyStore = defineStore('myStore', () => {
          // State - используй ref/reactive
          const items = ref([])
          const loading = ref(false)
        
          // Getters - используй computed
          const itemCount = computed(() => items.value.length)
        
          // Actions - обычные функции
          async function fetchItems() {
            loading.value = true
            try {
              items.value = await $fetch('/api/items')
            } finally {
              loading.value = false
            }
          }
        
          return { items, loading, itemCount, fetchItems }
        })
        
        ⚡ ПРОИЗВОДИТЕЛЬНОСТЬ:
        - Getters должны быть простыми (не O(n²))
        - Избегай глубокой реактивности где не нужна (shallowRef)
        - Actions должны быть async где возможно
        - Используй batch updates для множественных изменений
        
        🔒 БЕЗОПАСНОСТЬ:
        - Не храни чувствительные данные в state (пароли, полные токены)
        - Используй httpOnly cookies для auth токенов
        - Очищай чувствительные данные при logout
        
        📝 JAVASCRIPT:
        - Добавь JSDoc для store:
          /**
           * Store для управления корзиной
           * @returns {Object} Store API
           */
        - Валидируй параметры actions
        - Обрабатывай ошибки в actions (try/catch)
        
        📊 BREAKING CHANGES:
        - Проверь все компоненты, использующие этот store
        - Переименование state/getters/actions сломает зависимые компоненты
        - Покажи список затронутых файлов
        - Оцени влияние на тесты
        
        🐛 ЧАСТЫЕ ОШИБКИ:
        - Забыли return state/getters/actions из setup
        - Мутируют state напрямую вместо через actions
        - Синхронные actions для async операций
        - Не используют computed для getters (обычная функция)
        - Циркулярные зависимости между stores
    - path: "**/*.js"
      instructions: |
        Проверь JavaScript код на качество и архитектуру.
        
        🏗️ АРХИТЕКТУРА:
        - Функции должны быть чистыми где возможно (без side effects)
        - Модульность - одна функция = одна задача (Single Responsibility)
        - DRY принцип - нет дублирования кода
        - Правильная организация экспортов (named exports для утилит)
        
        📝 ДОКУМЕНТАЦИЯ:
        - JSDoc для всех экспортируемых функций:
          /**
           * Форматирует цену для отображения
           * @param {number} price - Цена в центах
           * @param {string} [currency='USD'] - Код валюты
           * @returns {string} Отформатированная строка цены
           * @example
           * formatPrice(1000, 'USD') // "$10.00"
           */
        - Комментарии для сложной бизнес-логики
        - TODO/FIXME с описанием проблемы
        
        ⚡ ПРОИЗВОДИТЕЛЬНОСТЬ:
        - Алгоритмическая сложность (O(n) лучше O(n²))
        - Нет создания функций/объектов в циклах
        - Эффективная работа с массивами:
          * map/filter/reduce вместо for loops где уместно
          * Set/Map вместо array.find в циклах
          * Избегай вложенных .find/.filter
        - Мемоизация дорогих вычислений
        
        🔒 БЕЗОПАСНОСТЬ:
        - Валидация входных данных (проверка типов, диапазонов)
        - Экранирование опасных символов
        - Нет eval() или new Function()
        - Санитизация HTML контента
        - Try/catch для всех async функций
        
        🐛 ОБРАБОТКА ОШИБОК:
        - Все async функции должны иметь try/catch
        - Promise.all должен обрабатывать частичные ошибки
        - Проверки на null/undefined перед использованием
        - Информативные сообщения об ошибках
        - Логирование ошибок (но не чувствительных данных)
        
        📊 КАЧЕСТВО КОДА:
        - Используй const по умолчанию, let только если нужна переприсваивание
        - Избегай var (проблемы с hoisting и scope)
        - Деструктуризация объектов и массивов
        - Spread operator вместо Object.assign
        - Optional chaining (?.) для безопасного доступа
        - Nullish coalescing (??) вместо ||
        - Template literals вместо конкатенации строк
        
        🐛 ЧАСТЫЕ ОШИБКИ В JAVASCRIPT:
        - == вместо === (приведение типов)
        - Забыли return в функции
        - Мутация параметров функции
        - Async без await
        - Promise без catch
        - Неправильный this в callbacks (используй arrow functions)
        - Сравнение объектов/массивов через ===
        - parseInt без radix (второй параметр)
        - Array.find когда нужен Array.some
        
        💡 BEST PRACTICES:
        - Магические числа должны быть константами с понятными именами:
          ❌ if (status === 200)
          ✅ const HTTP_OK = 200; if (status === HTTP_OK)
        - Ранний return для уменьшения вложенности:
          ❌ if (condition) { /* много кода */ } else { return }
          ✅ if (!condition) return; /* много кода */
        - Default параметры вместо проверок:
          ❌ function test(x) { x = x || 5 }
          ✅ function test(x = 5) { }
        - Деструктуризация с default:
          function fetchUser({ id, retry = 3 } = {}) { }
    - path: "**/api/**/*.js"
      instructions: |
        Проверь API слой на корректность (JavaScript).
        
        🏗️ АРХИТЕКТУРА API:
        - Этот слой ДОЛЖЕН быть единственным местом HTTP запросов
        - Компоненты и composables НЕ должны напрямую вызывать API
        - Используй единый API client (ofetch для Nuxt, axios)
        - Централизованная обработка ошибок
        - Централизованная обработка auth (токены, headers)
        
        📝 СТРУКТУРА API МОДУЛЯ:
        // api/users.js
        /**
         * API для работы с пользователями
         */
        
        const apiClient = $fetch.create({
          baseURL: '/api',
          // общие настройки
        })
        
        /**
         * Получить пользователя по ID
         * @param {string} id - ID пользователя
         * @returns {Promise<Object>} Данные пользователя
         */
        export async function getUser(id) {
          return await apiClient(`/users/${id}`)
        }
        
        ⚡ ПРОИЗВОДИТЕЛЬНОСТЬ:
        - Request batching где возможно (избегай N+1 проблему)
        - Используй AbortController для отмены запросов:
          const controller = new AbortController()
          fetch(url, { signal: controller.signal })
        - Кеширование повторяющихся запросов
        - Debounce для поисковых запросов
        - Pagination для больших списков
        
        🔒 БЕЗОПАСНОСТЬ:
        - API ключи из useRuntimeConfig(), НЕ в коде:
          const config = useRuntimeConfig()
          headers: { 'Authorization': `Bearer ${config.apiKey}` }
        - CSRF токены для мутирующих запросов (POST/PUT/DELETE)
        - Rate limiting awareness
        - Валидация response перед использованием:
          if (!response.ok) throw new Error(`HTTP ${response.status}`)
        - Timeout для запросов (предотвращение зависания)
        - Не логируй чувствительные данные (токены, пароли)
        
        🐛 ОБРАБОТКА ОШИБОК:
        - Try/catch для всех async функций
        - Информативные сообщения об ошибках
        - Разные типы ошибок (network, validation, server):
          class ApiError extends Error {
            constructor(message, status, data) {
              super(message)
              this.status = status
              this.data = data
            }
          }
        - Retry логика для временных сбоев (с exponential backoff)
        
        📊 RESPONSE HANDLING:
        - Проверяй response.ok перед парсингом
        - Валидируй структуру данных:
          if (!response.data || !Array.isArray(response.data.items)) {
            throw new Error('Invalid response format')
          }
        - Нормализация данных (приведение к единому формату)
        
        💡 ПРИМЕРЫ:
        - Покажи правильную обработку ошибок
        - Предложи типизацию через JSDoc
        - Укажи на отсутствие timeout
        - Покажи как добавить retry механизм

    # Utils
    - path: "**/utils/**/*.js"
      instructions: |
        Проверь утилиты на переиспользуемость и тестируемость (JavaScript).
        
        🏗️ АРХИТЕКТУРА:
        - Функции должны быть чистыми (pure functions - нет side effects)
        - Без внешних зависимостей где возможно
        - Хорошо документированы (JSDoc)
        - Один файл = одна категория утилит (formatters.js, validators.js)
        
        📝 ДОКУМЕНТАЦИЯ:
        /**
         * Форматирует дату в читаемый вид
         * @param {Date|string|number} date - Дата для форматирования
         * @param {Object} [options] - Опции форматирования
         * @param {string} [options.locale='ru-RU'] - Локаль
         * @param {string} [options.format='short'] - Формат (short|long|full)
         * @returns {string} Отформатированная дата
         * @throws {Error} Если date невалидная
         * @example
         * formatDate(new Date()) // "3 дек 2024"
         * formatDate('2024-12-03', { format: 'long' }) // "3 декабря 2024 года"
         */
        
        ⚡ ПРОИЗВОДИТЕЛЬНОСТЬ:
        - Оптимальные алгоритмы (не O(n²))
        - Нет ненужных клонирований объектов
        - Ленивые вычисления где уместно
        - Мемоизация для дорогих операций:
          const memoize = (fn) => {
            const cache = new Map()
            return (...args) => {
              const key = JSON.stringify(args)
              if (cache.has(key)) return cache.get(key)
              const result = fn(...args)
              cache.set(key, result)
              return result
            }
          }
        
        🔒 БЕЗОПАСНОСТЬ:
        - Валидация входных параметров
        - Защита от prototype pollution:
          if (key === '__proto__' || key === 'constructor') return
        - Санитизация строк для HTML/SQL
        - Безопасное клонирование объектов (structuredClone)
        
        🧪 ТЕСТИРУЕМОСТЬ:
        - Функции легко тестировать в изоляции
        - Нет скрытых зависимостей (все через параметры)
        - Детерминированное поведение (одинаковый input = одинаковый output)
        - Нет побочных эффектов (не меняют глобальное состояние)
        
        📊 BREAKING CHANGES:
        - Утилиты используются везде - проверь все места использования
        - Изменение сигнатуры функции - критично:
          * Добавление обязательного параметра
          * Изменение типа возвращаемого значения
          * Изменение поведения (breaking behavior)
        - Покажи impact analysis (сколько файлов затронуто)
        
        🐛 ЧАСТЫЕ ОШИБКИ:
        - Мутация входных параметров:
          ❌ function sort(arr) { arr.sort(); return arr }
          ✅ function sort(arr) { return [...arr].sort() }
        - Отсутствие проверок на null/undefined
        - Неправильная обработка edge cases (пустые массивы, null)
        - Забыли default параметры
        
        💡 BEST PRACTICES:
        - Именуй функции глаголами (formatDate, validateEmail)
        - Префиксы для boolean: is/has/should (isValid, hasAccess)
        - Функции делают одно действие (не formatAndValidateDate)
        - Константы в UPPER_CASE если экспортируются
        - Guard clauses для ранних выходов:
          if (!input) return null
          if (input.length === 0) return []
          // основная логика

    # Server API (Nuxt server routes)
    - path: "**/server/api/**/*.js"
      instructions: |
        Проверь Nuxt server API route (JavaScript).
        
        🏗️ АРХИТЕКТУРА:
        - Используй defineEventHandler для всех routes
        - Валидация входных данных обязательна
        - Централизованная обработка ошибок
        - Разделение по HTTP методам
        
        📝 СТРУКТУРА:
        export default defineEventHandler(async (event) => {
          // 1. Валидация метода
          if (event.method !== 'POST') {
            throw createError({
              statusCode: 405,
              message: 'Method not allowed'
            })
          }
        
          // 2. Получение и валидация данных
          const body = await readBody(event)
          if (!body.email) {
            throw createError({
              statusCode: 400,
              message: 'Email is required'
            })
          }
        
          // 3. Бизнес-логика
          const result = await processData(body)
        
          // 4. Возврат результата
          return { success: true, data: result }
        })
        
        🔒 БЕЗОПАСНОСТЬ:
        - Валидация всех входных данных (body, query, params)
        - Rate limiting для публичных endpoints
        - CORS настройки
        - SQL injection защита (используй параметризованные запросы)
        - XSS защита (санитизация HTML)
        - Авторизация/аутентификация где нужно
        - Не возвращай внутренние ошибки клиенту
        - Логируй попытки несанкционированного доступа
        
        🐛 ОБРАБОТКА ОШИБОК:
        - Try/catch для всех async операций
        - createError для HTTP ошибок:
          throw createError({
            statusCode: 404,
            message: 'Resource not found',
            data: { /* дополнительные данные */ }
          })
        - Информативные, но безопасные сообщения об ошибках
        - Логирование ошибок на сервере (не отправляй клиенту)
        
        ⚡ ПРОИЗВОДИТЕЛЬНОСТЬ:
        - Кеширование где возможно
        - Database connection pooling
        - Асинхронные операции параллельно (Promise.all)
        - Pagination для больших списков
        - Оптимизация database queries (indexes, EXPLAIN)
        
        💡 BEST PRACTICES:
        - RESTful conventions (GET/POST/PUT/DELETE)
        - Правильные HTTP статус коды (200, 201, 400, 404, 500)
        - Консистентный формат ответов
        - Версионирование API (/api/v1/)

  # 🎯 КАСТОМНЫЕ ПРАВИЛА
  custom_rules:
    # API вызовы только в разрешенных местах
    - name: "api_calls_location"
      severity: "error"
      description: "API вызовы должны быть только в /api, /composables или /server"
      pattern: "(?:fetch|axios|\\$fetch|useFetch)\\s*\\("
      exclude_paths:
        - "**/api/**"
        - "**/composables/**"
        - "**/server/**"
      include_paths:
        - "**/components/**"
        - "**/pages/**"
        - "**/layouts/**"
      message: |
        🔴 АРХИТЕКТУРНАЯ ОШИБКА: Прямой API вызов в UI слое
        
        API вызовы разрешены только в:
        ✅ composables/ - для переиспользуемой логики
        ✅ api/ - для API клиентов
        ✅ server/ - для серверной логики
        
        ❌ Запрещены в:
        ✗ components/ - только UI логика
        ✗ pages/ - только композиция компонентов
        ✗ layouts/ - только структура
        
        💡 Решение:
        Создай composable:
        
        // composables/useYourFeature.js
        export function useYourFeature() {
          const data = ref(null)
          const isLoading = ref(false)
        
          const fetchData = async () => {
            isLoading.value = true
            try {
              data.value = await $fetch('/api/...')
            } finally {
              isLoading.value = false
            }
          }
        
          return { data, isLoading, fetchData }
        }
    - name: "no_secrets_in_code"
      severity: "error"
      description: "Секреты не должны быть в коде"
      pattern: "(?:api[_-]?key|secret|token|password)\\s*[:=]\\s*['\"][A-Za-z0-9_-]{20,}['\"]"
      message: |
        🔴 КРИТИЧЕСКАЯ УЯЗВИМОСТЬ: Секрет найден в коде!
        ❌ Никогда не храни в коде:
        - API ключи
        - Токены доступа
        - Пароли
        - Приватные ключи
